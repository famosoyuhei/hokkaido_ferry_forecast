# Templates Agent Guide

Flask/Jinjaテンプレートを編集する時だけ読む。

## Rules

- `forecast_dashboard.py` 側のAPI契約とテンプレート表示を同期する。
- 航路名・便数・夏季便の表示はハードコードせず、サーバーから渡された当日ダイヤ由来の値を使う。
- 存在しない稚内-沓形直行便をUIに出さない。
- モバイル表示で重要な運航・欠航・リスク情報が折り返しで読めることを優先する。
- テンプレートだけの変更でも、可能なら `/api/stats` と主要画面を確認する。
