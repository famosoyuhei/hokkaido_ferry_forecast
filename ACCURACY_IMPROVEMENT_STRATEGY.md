# 精度向上戦略：GitHub Actions最大活用プラン

**作成日**: 2026-01-19
**目的**: 気象データ精度と運航欠航予報精度を同時に向上

---

## 🎯 現状分析

### データフロー現状
```
GitHub Actions (定期実行)
  ├─ 気象予報収集 (1日4回) ✅
  ├─ 実運航データ収集 (1日1回) ✅
  └─ 精度追跡 (1日1回) ⚠️ データ蓄積が進んでいない
```

### 問題点
1. **精度追跡データが蓄積されていない**
   - `operation_accuracy`: 0件
   - `weather_accuracy`: 0件
   - 原因: データベース参照の不整合

2. **フィードバックループが機能していない**
   - 予測 → 実績 → 精度計算 → 閾値調整のサイクルが回っていない

3. **データソースの多様性不足**
   - JMA + Open-Meteoのみ
   - 実測データとの比較が不十分

---

## 📊 精度向上の3つの柱

### 1. データ収集の強化
### 2. 精度計算の自動化
### 3. 機械学習による閾値最適化

---

## 🔧 実装プラン

## Phase 1: データ収集強化（優先度: 高）

### 1.1 複数気象データソースの統合

**新規GitHub Actions**:
```yaml
name: Multi-Source Weather Collection
on:
  schedule:
    - cron: '0 */3 * * *'  # 3時間ごと（1日8回）

jobs:
  collect-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Collect JMA Data
      - name: Collect Open-Meteo Data
      - name: Collect Weather Underground (オプション)
      - name: Collect Visual Crossing (オプション)
      - name: Calculate Ensemble Average
      - name: Store with confidence scores
```

**メリット**:
- 複数ソースの平均で精度向上（アンサンブル予測）
- データソース別の精度追跡
- 信頼度スコア付きデータ

### 1.2 実測気象データの収集

**新規スクリプト**: `actual_weather_collector.py`
```python
# 稚内地方気象台の実測データを収集
# - 風速（10分平均、最大瞬間）
# - 波高（実測）
# - 視程
# - 気温、気圧、降水量
```

**データソース候補**:
1. 気象庁過去の気象データ検索
2. 稚内地方気象台API（存在すれば）
3. 海上保安庁の波浪観測データ

**GitHub Actions**:
```yaml
name: Actual Weather Collection
on:
  schedule:
    - cron: '0 1 * * *'  # 毎日01:00 JST（前日データ確定後）
```

### 1.3 フェリー会社の欠航理由詳細取得

**改善**: `improved_ferry_collector.py`
- 欠航理由テキストのパース強化
- 「強風」「高波」「濃霧」などキーワード抽出
- 時刻情報の正確な取得

---

## Phase 2: 精度計算システムの改善（優先度: 高）

### 2.1 データベース統合スクリプト

**新規**: `unified_accuracy_tracker.py`

**機能**:
1. 複数DBの統合参照
   ```python
   # ferry_weather_forecast.db: 予測データ
   # heartland_ferry_real_data.db: 実運航データ
   # 両者をマッチング
   ```

2. 3種類の精度計算
   - **運航予測精度**: 予測リスクレベル vs 実際の運航状況
   - **気象予測精度**: 予測気象値 vs 実測気象値
   - **閾値適合度**: 現在の閾値が最適か

3. 日次・週次・月次サマリー生成

**GitHub Actions統合**:
```yaml
name: Comprehensive Accuracy Tracking
on:
  schedule:
    - cron: '0 22 * * *'  # 毎日07:00 JST
  workflow_dispatch:  # 手動実行可能

jobs:
  track-accuracy:
    steps:
      - name: Collect actual weather
      - name: Match predictions with reality
      - name: Calculate operation accuracy
      - name: Calculate weather accuracy
      - name: Generate daily report
      - name: Update dashboard
      - name: Commit results to repo
```

### 2.2 精度メトリクスの拡充

**現在**: 正解率のみ
**追加メトリクス**:

1. **混同行列（Confusion Matrix）**
   ```
                実際
              運航  欠航
   予 運航予想  TP   FP
   測 欠航予想  FN   TN
   ```

2. **適合率（Precision）**: 欠航予想のうち実際に欠航した割合
3. **再現率（Recall）**: 実際の欠航のうち予想できた割合
4. **F1スコア**: 適合率と再現率の調和平均
5. **リスクレベル別精度**:
   - HIGH予測の的中率
   - MEDIUM予測の的中率
   - LOW予測の的中率
   - MINIMAL予測の的中率

6. **気象要素別精度**:
   - 風速予測誤差（RMSE, MAE）
   - 波高予測誤差
   - 視程予測誤差

7. **時間帯別精度**:
   - 1日前予測
   - 3日前予測
   - 7日前予測

---

## Phase 3: 機械学習による最適化（優先度: 中）

### 3.1 動的閾値調整システム

**新規**: `threshold_optimizer.py`

**アルゴリズム**:
```python
def optimize_thresholds():
    # 過去30日間のデータで最適閾値を計算

    # 1. グリッドサーチ
    for wind_threshold in range(10, 30, 2):
        for wave_threshold in range(1.5, 4.0, 0.5):
            accuracy = calculate_accuracy(wind_threshold, wave_threshold)

    # 2. F1スコア最大化
    best_thresholds = find_max_f1_score()

    # 3. 季節性考慮
    if is_winter():
        adjust_for_ice_conditions()

    # 4. 適用前のシミュレーション
    simulate_impact(new_thresholds)

    # 5. 段階的適用（A/Bテスト）
    return optimized_thresholds
```

**GitHub Actions**:
```yaml
name: Weekly Threshold Optimization
on:
  schedule:
    - cron: '0 15 * * 0'  # 毎週日曜 00:00 JST

jobs:
  optimize:
    steps:
      - name: Analyze last 30 days
      - name: Calculate optimal thresholds
      - name: Run simulation
      - name: Generate report
      - name: Auto-apply if improvement > 5%
      - name: Create PR for review
```

### 3.2 機械学習モデルの導入

**Option A: 軽量モデル（GitHub Actions内で実行可能）**

```python
from sklearn.ensemble import RandomForestClassifier

# 特徴量
features = [
    'wind_speed_forecast',
    'wave_height_forecast',
    'visibility_forecast',
    'temperature',
    'pressure',
    'wind_direction',
    'time_of_day',
    'season',
    'day_of_week',
    'last_3days_cancellation_rate'
]

# ターゲット
target = 'is_cancelled'

# モデル訓練
model = RandomForestClassifier(n_estimators=100)
model.fit(X_train, y_train)

# 毎日再訓練
```

**Option B: 時系列予測モデル（LSTMなど）**
- より高度だが計算コスト高い
- 外部サービス（AWS Lambda, Google Cloud Functions）で実行

**GitHub Actions統合**:
```yaml
name: ML Model Training
on:
  schedule:
    - cron: '0 16 * * 0'  # 毎週日曜 01:00 JST
  workflow_dispatch:

jobs:
  train-model:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare training data
      - name: Train Random Forest model
      - name: Evaluate model
      - name: Compare with current system
      - name: Deploy if better
      - name: Archive model version
```

---

## Phase 4: フィードバックループの自動化（優先度: 中）

### 4.1 自動レポート生成

**新規**: `accuracy_report_generator.py`

**生成レポート**:
1. **日次レポート** (毎朝07:30 JST)
   - 昨日の予測精度
   - 誤予測の分析
   - 気象データの精度

2. **週次レポート** (毎週月曜 08:00 JST)
   - 週間精度トレンド
   - 閾値調整の推奨
   - 異常検知

3. **月次レポート** (毎月1日 09:00 JST)
   - 月間統計
   - 季節性分析
   - 改善提案

**配信方法**:
- GitHub Issues自動作成
- メール通知（GitHub Actions Secrets経由）
- Slackインテグレーション（オプション）

### 4.2 異常検知システム

**新規**: `anomaly_detector.py`

**検知項目**:
1. 精度の急激な低下（前週比 -10%以上）
2. データ収集の失敗（3回連続）
3. 予測と実績の乖離（3σ以上）
4. APIエラー率の上昇

**アクション**:
- GitHub Issue自動作成（ラベル: `alert`）
- 緊急メール通知
- 自動ロールバック（閾値変更の場合）

**GitHub Actions**:
```yaml
name: Anomaly Detection
on:
  schedule:
    - cron: '*/30 * * * *'  # 30分ごと

jobs:
  detect:
    steps:
      - name: Check data collection health
      - name: Check accuracy metrics
      - name: Detect anomalies
      - name: Create alert if needed
```

---

## Phase 5: データ可視化とモニタリング（優先度: 低）

### 5.1 精度ダッシュボードの拡充

**追加グラフ**:
1. 精度トレンド（時系列）
2. 混同行列（ヒートマップ）
3. リスクレベル別精度
4. 気象要素別RMSE
5. データソース別精度比較

### 5.2 GitHub Pages活用

**新規**: `.github/workflows/deploy-accuracy-dashboard.yml`

```yaml
name: Deploy Accuracy Dashboard to GitHub Pages
on:
  schedule:
    - cron: '0 */6 * * *'  # 6時間ごと

jobs:
  deploy:
    steps:
      - name: Generate static HTML report
      - name: Create charts with Plotly
      - name: Deploy to gh-pages branch
```

**アクセス**: `https://famosoyuhei.github.io/hokkaido_ferry_forecast/accuracy/`

---

## 📅 実装スケジュール（推奨）

### Week 1-2: Phase 1（データ収集強化）
- [ ] 実測気象データ収集スクリプト作成
- [ ] GitHub Actions設定
- [ ] テスト実行

### Week 3-4: Phase 2（精度計算改善）
- [ ] unified_accuracy_tracker.py作成
- [ ] メトリクス拡充
- [ ] データベース統合ロジック

### Week 5-6: Phase 3（機械学習導入）
- [ ] Random Forestモデル実装
- [ ] 訓練データ準備
- [ ] モデル評価

### Week 7-8: Phase 4（自動化）
- [ ] レポート生成システム
- [ ] 異常検知システム
- [ ] 通知設定

### Week 9-10: Phase 5（可視化）
- [ ] ダッシュボード拡充
- [ ] GitHub Pages設定

---

## 💰 コスト分析

### GitHub Actions無料枠
- **Public repo**: 無制限
- **Private repo**: 月2,000分

### 推定使用量（月間）
```
データ収集:
  - 気象予報（1日8回 × 5分） = 1,200分/月
  - 実運航（1日1回 × 3分） = 90分/月
  - 実測気象（1日1回 × 5分） = 150分/月

精度計算:
  - 日次（1日1回 × 10分） = 300分/月
  - 週次（月4回 × 30分） = 120分/月

ML訓練:
  - 週次（月4回 × 20分） = 80分/月

合計: 1,940分/月
```

**結論**: Public repoなら無料、Private repoでも無料枠内

---

## 🎯 期待される効果

### 短期（1-2ヶ月）
- 精度計算の自動化 ✅
- データ蓄積の開始 ✅
- 基本メトリクスの可視化 ✅

### 中期（3-6ヶ月）
- 精度10-15%向上（予想）
- 動的閾値調整の稼働
- 異常検知の自動化

### 長期（6-12ヶ月）
- 精度20%以上向上（予想）
- 機械学習モデルの導入
- 完全自動化されたフィードバックループ

---

## 🚀 クイックスタート

### 最小限の実装（今すぐ開始可能）

1. **unified_accuracy_tracker.py作成**
   ```bash
   # データベース統合
   # 精度計算
   # サマリー生成
   ```

2. **GitHub Actionsに追加**
   ```yaml
   name: Daily Accuracy Tracking
   on:
     schedule:
       - cron: '0 22 * * *'
   ```

3. **1週間運用してデータ確認**

4. **Phase 2以降を段階的に実装**

---

**最終更新**: 2026-01-19
**ステータス**: 提案・未実装
