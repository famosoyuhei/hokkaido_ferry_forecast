# 精度向上戦略

**最終更新**: 2026-04-19

---

## 実装済み（Phase 1-2）

### Phase 1: データ収集強化 ✅
- **実測気象データ収集**: `actual_weather_collector.py`（Open-Meteo Archive API / Marine Archive API）
- **波高の実測値取得**: Marine APIで実測値を取得（以前は1.5m固定だったバグを修正）
- **フェリースクレイパー修正**: ページタイトルで全便欠航と誤判定するバグを修正（2026-04-05）
- **歴史的データのバックフィル**: `backfill_actual_weather.py`で2025-10-01〜前日分を収集

### Phase 2: 精度計算システム ✅
- **Hindcast精度追跡**: `unified_accuracy_tracker.py`（実測気象 → リスクモデル → 実運航比較）
- **高度なメトリクス**: 混同行列、適合率、再現率、F1スコア
- **日次自動実行**: GitHub Actions（毎日07:00 JST）
- **信頼できるデータ期間**: 2026-04-05以降（スクレイパー修正後）

**現状の精度**: 直近7日間（2026-04-12〜18）で100%。4月上旬の気象外欠航（冬→夏ダイヤ切替）を除けば安定稼働中。

---

## 将来計画（Phase 3-5）

### Phase 3: 機械学習による最適化（未実装）
- 動的閾値調整システム（`threshold_optimizer.py`）
- Random Forestモデルによる欠航確率予測
- データが十分蓄積された時点（目安: 3ヶ月以上）で実施

### Phase 4: フィードバックループの自動化（未実装）
- 日次・週次・月次精度レポートの自動生成
- 異常検知システム（精度急低下時のアラート）

### Phase 5: データ可視化の強化（未実装）
- 精度トレンドグラフの追加
- GitHub Pagesへのレポート自動デプロイ

---

## 教訓

- **誤ったデータで閾値調整しても意味がない**（季節調整を導入したが、欠航データのバグが原因だったため逆効果だった）
- まず正しいデータを確保してから閾値をチューニングすること
- 気象外欠航（ダイヤ切替・機材不具合等）は気象モデルで予測不可能なので、データ蓄積で季節パターンとして把握する
